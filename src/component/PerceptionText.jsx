/* eslint-disable react/prop-types */
function PerceptionText({ title }) {
  return (
    <div className="perceptiontext">
      <p>{title}</p>
      <p>{title}</p>
    </div>
  );
}

export default PerceptionText;
